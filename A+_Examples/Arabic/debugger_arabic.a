// مثال المصحح بالعربية

دالة احسب(أ, ب) {
    تتبع("احسب: أ = " + أ + ", ب = " + ب)
    متغير مجموع = أ + ب
    مصحح  // يتوقف هنا ويعرض المتغيرات
    رجع مجموع * 2
}

متغير س = 10
متغير ص = 20
تتبع("قبل الاستدعاء")
متغير ن = احسب(س, ص)
اطبع("النتيجة: " + ن)
تتبع("تم بنجاح")
